steps:
- name: gcr.io/cloud-builders/git
  entrypoint: 'bash'
  args: ['startup.sh']
  secretEnv: ['GITHUB_TOKEN']
- name: goreleaser/goreleaser:latest
  script: |
    #!/usr/bin/env bash
    export GODEBUG='http2debug=2'
    source /github_assets/release_env
    exec env goreleaser release --clean --verbose
availableSecrets:
  secretManager:
  - versionName: projects/tomburke-gcf-source-repo/secrets/GITHUB_TOKEN/versions/3
    env: 'GITHUB_TOKEN'
options:
  logging: CLOUD_LOGGING_ONLY
  volumes:
  - name: 'github-assets'
    path: '/github_assets'